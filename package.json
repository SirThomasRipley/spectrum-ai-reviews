{
  "name": "spectrum-ai-reviews",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "build:cloudflare": "npm run clean && next build",
    "build:static": "next build && next export",
    "start": "next start",
    "lint": "next lint",
    "clean": "rm -rf .next out",
    "preview": "npm run build && npm run start",
    "deploy": "wrangler pages deploy out",
    "deploy:preview": "wrangler pages deploy out --branch preview",
    "check-types": "tsc --noEmit",
    "format": "prettier --write .",
    "analyze": "ANALYZE=true next build"
  },
  "dependencies": {
    "lucide-react": "^0.544.0",
    "next": "14.2.23",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "critters": "^0.0.23",
    "eslint": "^8.57.1",
    "eslint-config-next": "^14.2.23",
    "postcss": "^8",
    "tailwindcss": "^3.4.1"
  }
}
